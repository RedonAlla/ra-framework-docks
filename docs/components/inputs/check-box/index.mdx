---
id: ra-checkbox
title: Check Box
sidebar_position: 2
last_update:
  date: 2024-03-29
  author: Redon Alla
tags: [react, react-native, expo, framework, component]
---

The CheckBox is typically used to represent boolean values via a binary checked state

**Check Box** is package is part of **ra-inputs** for React Native, it provides a same base inputs UI library and functionality.

**RA Check Box** is part of the [**ra-inputs**](./) package.
To use the **RA Check Box** you needs to install `ra-inputs` package.
[Fallow this link](./#installation) on how to install the package.

## Dependencies
- `ra-theme-context`
- `ra-icons`
- `ui-constants`

import Installation from '../../_installation.mdx';

<Installation title="RA Inputs" />

```bash npm2yarn
npm install C:/ReactNative/framework/ra-inputs
```

## API

```jsx
import { Check, CheckProps } from 'ra-inputs';
```

## Component

**Check**

Type: `React.PureComponent<CheckProps<T>>`

